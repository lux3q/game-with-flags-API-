{
  "name": "fun-with-flags",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:build": "node-sass --include-path scss scss/index.scss css/index.css",
    "sass:watch": "nodemon -e scss -x \"npm run sass:build\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.3",
    "nodemon": "^2.0.20"
  }
}
